{% extends 'layout.html.twig' %}
{% block title %}
    MVC - Items
{% endblock %}

{% block content %}
    <h1>Items</h1>
    <ul>
        {% for item in items %}
            <li><a href="/item/{{ item.id }}">{{ item.id }}. {{ item.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
